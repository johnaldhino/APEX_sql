APEX mysql scripts, desisnged for jlab computing system.


Directories can be changed to work on local systems by altering scripts/Directories.py

logfiles directory should contain halog files from begining and end of runs.

scripts directory split into:
-
